<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walkingschoolbus">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".GroupManagementActivity" />
        <activity android:name=".AddNewParentsActivity" />
        <activity android:name=".MonitoringListActivity" />
        <activity android:name=".PlacePickerActivity" />
        <activity android:name=".MonitoredListActivity" />
        <activity android:name=".MonitorActivity" />
        <activity android:name=".LeaderActivity" />
        <activity android:name=".OnMarkerClickActivity" />
        <activity android:name=".RemoveMonitoringUserFromGroup" />
        <activity android:name=".MyKidsActivity" />
        <activity android:name=".OpenKidActivity" />
        <activity android:name=".AddNewKidActivity" />
        <activity android:name=".MyParentsActivity" />
        <activity android:name=".ViewUserSettingsActivity" />
        <activity android:name=".EditUserSettingsActivity" />
        <activity android:name=".AddKidToGroupActivity" />
        <activity android:name=".OnMarkerClickKidActivity" />
        <activity android:name=".OpenKidGroupActivity" />
        <activity android:name=".ViewUserMonitoredByActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".SendMessageActivity" />
        <activity android:name=".emergencyMessageActivity" />
        <activity android:name=".MessageDetailActivity" />
        <activity android:name=".MemberActivity" />
        <activity android:name=".ParentsDashboardActivity" />
        <activity android:name=".GameActivity"/>
        <activity android:name=".LeaderBoardActivity"></activity>
        <activity android:name=".PermissionSystem"></activity>
        <activity android:name=".PermissionListActivity"></activity>
    </application>

</manifest>